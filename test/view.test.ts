/// <reference path="../typings/mocha/mocha.d.ts" />
///<reference path="../typings/chai/chai.d.ts"/>
///<reference path="../src/View.ts"/>

let assert = chai.assert;

describe('View', () => {
    let view: Headlight.IView;

    beforeEach(() => {
        view = new Headlight.View();
    });

    describe('events()', () => {



    });
});
